% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_pair_marker_analysis}
\alias{run_pair_marker_analysis}
\title{Run the complete pair marker analysis workflow}
\usage{
run_pair_marker_analysis(
  expr_file,
  clinical_file,
  out_dir = NULL,
  gene_col = NULL,
  response_col = "response",
  response_label = "response",
  main_delta = 0.25,
  delta_list = c(0, 0.25, 0.5),
  dataset_name = tools::file_path_sans_ext(basename(expr_file)),
  sig_cutoff = 0.05,
  min_nonzero_prop = 0.5,
  min_prop = 0.05,
  max_prop = 0.95,
  chisq_cutoff = 0.01,
  min_valid_prop = 0.5
)
}
\arguments{
\item{expr_file}{Path to expression table.}

\item{clinical_file}{Path to clinical table.}

\item{out_dir}{Output directory. If \code{NULL}, results are not written.}

\item{gene_col}{Gene column name in expression table.}

\item{response_col}{Clinical response column.}

\item{response_label}{Label treated as response.}

\item{main_delta}{Delta cutoff for the main analysis.}

\item{delta_list}{Delta cutoffs for sensitivity analysis.}

\item{dataset_name}{Prefix for output file names.}

\item{sig_cutoff}{Adjusted p-value cutoff for significant pairs.}

\item{min_nonzero_prop}{Minimum non-zero expression proportion.}

\item{min_prop}{Minimum pairwise comparison proportion.}

\item{max_prop}{Maximum pairwise comparison proportion.}

\item{chisq_cutoff}{Chi-square p-value cutoff.}

\item{min_valid_prop}{Minimum valid sample proportion.}
}
\value{
A list with filtered expression matrix, aligned clinical data,
response vector, sensitivity summary, all pairs, significant pairs, and
output paths.
}
\description{
Run the complete pair marker analysis workflow
}
\examples{
expr_file <- system.file(
  "extdata",
  "example_exp.txt",
  package = "immunePair"
)
clinical_file <- system.file(
  "extdata",
  "example_clinical.txt",
  package = "immunePair"
)

result <- run_pair_marker_analysis(
  expr_file = expr_file,
  clinical_file = clinical_file,
  out_dir = tempdir(),
  response_col = "response",
  response_label = "response",
  main_delta = 0.25,
  delta_list = c(0, 0.25, 0.5),
  dataset_name = "example"
)

result$sensitivity
head(result$pairs)
}
